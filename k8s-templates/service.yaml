apiVersion: v1
kind: Service
metadata:
  name: "{{API_NAME}}-service"
spec:
  type: NodePort
  selector:
    app: {{API_NAME}}
  ports:
    - protocol: TCP
      port: 80         # Internal port
      targetPort: 3000 # The port your Node app listens on