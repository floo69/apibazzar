apiVersion: v1
kind: Service
metadata:
  name: "tmdb-service"
spec:
  type: NodePort
  selector:
    app: tmdb
  ports:
    - protocol: TCP
      port: 80         # Internal port
      targetPort: 3000 # The port your Node app listens on