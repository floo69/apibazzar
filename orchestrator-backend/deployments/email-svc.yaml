apiVersion: v1
kind: Service
metadata:
  name: "email-service"
spec:
  type: NodePort
  selector:
    app: email
  ports:
    - protocol: TCP
      port: 80         # Internal port
      targetPort: 3000 # The port your Node app listens on